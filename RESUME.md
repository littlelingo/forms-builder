# Resume

## Workspace

- Repo: `/Users/clint/Workspace/forms-builder`
- Current focus: `Phase 2A - promote reviewed imports into durable authoring projects`
- Baseline target: `VA.gov-style web form flow`, not PDF round-trip

## Current State

- Phase 1 review is still live:
  - browser PDF upload and local corpus sample import both work
  - backend classification/extraction and page-image preview are live
  - review UI still supports page-by-page evidence inspection, review-state updates, and raw draft inspection
- Phase 2A foundation is now live:
  - reviewed conversions can be promoted with `POST /conversions/{id}/promote`
  - promotion creates a durable authoring project under `data/projects/<project-id>/`
  - persisted project artifacts are split into `project.json`, `document.json`, `source-context.json`, and revision snapshots under `revisions/`
  - authoring JSON is now separate from extraction JSON and is the new source of truth after promotion
- The frontend has been reset into a staged workflow:
  - `Import + Review` makes the source preview dominant and keeps mapping details secondary
  - `Build` combines flow shaping, step editing, and first-pass logic editing
  - `Publish` is now a distinct end-state screen rather than being mixed into authoring
  - `Open JSON` is now a first-class intake path alongside `Import PDF`

## What Was Just Completed

- Added shared authoring contracts in:
  - [apps/api/src/form_builder_api/models/authoring.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/models/authoring.py)
  - [packages/schema/src/authoring.ts](/Users/clint/Workspace/forms-builder/packages/schema/src/authoring.ts)
- Added reviewed-conversion promotion and file-backed project persistence:
  - [apps/api/src/form_builder_api/services/authoring.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/services/authoring.py)
  - [apps/api/src/form_builder_api/repository.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/repository.py)
  - [apps/api/src/form_builder_api/main.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/main.py)
- Added project APIs:
  - `GET /projects`
  - `GET /projects/{id}`
  - `PATCH /projects/{id}`
  - `GET /projects/{id}/document`
  - `PUT /projects/{id}/document`
  - `GET /projects/{id}/source-context`
  - `GET /projects/{id}/revisions`
- Replaced the monolithic review-only web app with a dual-workspace UI in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - explicit `Promote to project` gate
  - project list and project loading
  - hybrid authoring surface with structure tree, VA-baseline live preview, and inspector
  - first editing loop for step/section/group/field updates, add/remove, reordering, field-type switching, option editing, and section/group reparenting
- Replaced that first Phase 2 UI pass with a more guided staged application flow in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - `Import + Review`, `Build`, and `Publish` are now explicit top-level stages
  - the review screen is source-dominant, with structure/page mapping ahead of issues/confidence
  - the builder now combines flow editing, realistic VA step preview, and logic editing in one stage
  - the builder left rail is tabbed between `Flow` and `Imported`
  - drag and drop is now first-class for steps, sections, groups, and fields
  - imported source context moved into a collapsible drawer instead of a permanent side-by-side pane
- Tightened the builder interaction model in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - removed add-section/add-field controls from the builder header and moved editing actions closer to the step canvas
  - narrowed the builder sidebar to a fixed-width accordion outline so it no longer consumes roughly half of the workspace
  - removed the low-value `Imported` sidebar mode; the sidebar is now organization-first and source context stays in the drawer
  - made step-preview selection primary for sections, groups, and fields, with inspector updates driven from canvas clicks
  - moved document-title editing out of the inspector and into the preview header
  - replaced the inline rule expander with a bounded modal rule editor so logic editing cannot blow out the inspector column
  - added runtime-navigation preview buttons (`Previous`, `Continue` / `Submit`) at the bottom of the step canvas
  - runtime preview navigation now actually moves through steps and disables/enables appropriately
  - added a lightweight authored action-button primitive via `rendererHints.component = "button"` so buttons can be inserted into sections/groups before the full actions-events model lands
- Tightened the builder again around a step-first editing model in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - replaced the expanded builder tree with a compact step strip that only lists draggable steps plus `Add step`
  - kept the real editing work in the step preview and inspector instead of the sidebar
  - added page-style step cards so the left rail behaves more like a page strip than a nested outline
  - exposed `Button` directly in the field-type inspector so authored runtime controls are discoverable without knowing the old action-button path
- Tightened builder and publish controls around real operator behavior in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - defaulted the inspector to the wider column and moved inspector switching to icon-only controls with hover titles
  - made the builder save button always available so save state is clearer even when the project is already persisted
  - removed the special canvas-only action-button add path so buttons now read more like a normal field-type choice
  - replaced the bulky publish screen with a leaner save-and-release stage that shows actual stored file paths and a reversible published/unpublished toggle
- Locked the runtime behavior contract in shared schema and docs:
  - added shared runtime event/action/listener/host-binding/session-state contracts in [packages/schema/src/runtime.ts](/Users/clint/Workspace/forms-builder/packages/schema/src/runtime.ts)
  - extended [packages/schema/src/authoring.ts](/Users/clint/Workspace/forms-builder/packages/schema/src/authoring.ts) so authoring nodes and documents can optionally carry runtime behavior metadata
  - added backend model parity in [apps/api/src/form_builder_api/models/runtime.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/models/runtime.py) and [apps/api/src/form_builder_api/models/authoring.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/models/authoring.py)
  - added initial runtime contract docs in [docs/runtime-architecture.md](/Users/clint/Workspace/forms-builder/docs/runtime-architecture.md) and [docs/runtime-schema.md](/Users/clint/Workspace/forms-builder/docs/runtime-schema.md)
- Added the first shared runtime package and wired the builder preview into it:
  - created [packages/runtime/src/engine.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.ts), [packages/runtime/src/document-index.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/document-index.ts), [packages/runtime/src/session-state.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/session-state.ts), and [packages/runtime/src/validation.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/validation.ts)
  - the runtime now indexes authoring nodes, initializes session state, evaluates conditional-rule guards, validates required/basic field rules, executes built-in actions, and emits runtime events
  - existing button fields now get implicit `component.click` listeners so the current builder model still works before the dedicated `Events` tab lands
  - the builder preview in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx) now mounts the shared runtime, drives `Previous` / `Continue` / `Submit` through runtime actions, syncs preview step selection from `step.enter`, and dispatches authored button clicks as real runtime events
  - refreshed the workspace install with `npm install` so Vite resolves `@form-builder/runtime` during live dev-server validation
- Added the first guided runtime authoring surface in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - inspector now has a dedicated `Events` mode alongside `Properties` and `Logic`
  - `Form events` is now an explicit scope inside the inspector, so authors can edit form-level listeners without leaving the builder
  - form-level presets now support `Emit event on load`, `Emit event on submit`, and `Emit event on validation failure`
  - selected fields/components now expose node-level listener/action editing, with button and field presets designed for guided no-code authoring first
  - the old button-action dropdown in `Properties` was removed as the primary editing path and replaced with a handoff into `Events`
  - explicit button listeners now override the compatibility-only implicit button listener path in the runtime index
- Added dev-only runtime roundtrip tooling in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - builder header now exposes `Runtime tools` alongside normal authoring controls
  - the modal can export the live runtime session JSON and import it back into the mounted runtime
  - the modal shows the current submit payload, current session snapshot, and a recent runtime event trace
  - mock host-response controls exist for `success` and `error` flows while the shared runtime is still hosted only inside the builder preview
- Added deterministic runtime roundtrip coverage in [packages/runtime/src/engine.test.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.test.ts):
  - session export/import style restore
  - validation-blocked submit
  - submit success/error host roundtrip
  - explicit authored button listeners overriding the old implicit compatibility path
- Fixed a real runtime engine bug in [packages/runtime/src/engine.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.ts):
  - runtime-built events were being traced and emitted twice because `buildEvent` and `routeEvent` both pushed them through the bus
  - trace/event emission now runs through `routeEvent`, which removed duplicate `form.load`, `step.enter`, `form.validation_failed`, and custom-event entries from the runtime tools modal
- Added runtime roundtrip guidance in [docs/runtime-roundtrip-testing.md](/Users/clint/Workspace/forms-builder/docs/runtime-roundtrip-testing.md)
- Added light payload authoring in the `Events` tab in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - `emit_event` and `host_action` now support guided key/value payload editing by default
  - each payload editor also supports a raw JSON mode with explicit apply/reset controls
  - the key/value editor now seeds visible placeholder keys like `field_1` so payload rows are actually usable in the live inspector
- Tightened the runtime QA loop in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - preview `form.submit` no longer forces an immediate stage switch to `Publish`, so the host-response loop can be validated in place
  - `Runtime tools` now includes `Seed required values` so submit success/error flows can be exercised without hand-filling long imported forms first
  - live browser validation now covers authored button dispatch, `form.submit`, `form.submit_success`, and `form.submit_error`
- Added deeper runtime documentation in:
  - [docs/runtime-authoring-guide.md](/Users/clint/Workspace/forms-builder/docs/runtime-authoring-guide.md)
  - [docs/runtime-host-integration.md](/Users/clint/Workspace/forms-builder/docs/runtime-host-integration.md)
  - [docs/runtime-cookbook.md](/Users/clint/Workspace/forms-builder/docs/runtime-cookbook.md)
  - refreshed [docs/runtime-roundtrip-testing.md](/Users/clint/Workspace/forms-builder/docs/runtime-roundtrip-testing.md) so the current validation status and remaining gaps reflect the actual runtime/browser state
- Tightened the shell toward a shadcn-style default UI system:
  - updated shared cards and badges in [packages/ui/src/components/panel-card.tsx](/Users/clint/Workspace/forms-builder/packages/ui/src/components/panel-card.tsx), [packages/ui/src/components/status-badge.tsx](/Users/clint/Workspace/forms-builder/packages/ui/src/components/status-badge.tsx), and [packages/ui/src/components/metric-tile.tsx](/Users/clint/Workspace/forms-builder/packages/ui/src/components/metric-tile.tsx)
  - shifted global shell styling in [apps/web/src/styles.css](/Users/clint/Workspace/forms-builder/apps/web/src/styles.css) to a denser neutral surface, subtler shadows, and tighter pills
  - tightened builder/review chrome in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx) with smaller stage headers, more compact action geometry, denser stage tabs, refined iconography, and lower panel minimum heights
  - this pass keeps the app/builder shell on the shadcn-style side while leaving the step/runtime surface as the separate output-facing layer
- Replaced the previous visual system with a cleaner blue/neutral product palette in [apps/web/src/styles.css](/Users/clint/Workspace/forms-builder/apps/web/src/styles.css)
- Added backend regression coverage for promotion, disk reload, and direct authoring-JSON import in [apps/api/tests/test_smoke.py](/Users/clint/Workspace/forms-builder/apps/api/tests/test_smoke.py)
- Added persistence-style runtime regression coverage in [packages/runtime/src/engine.test.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.test.ts) and [apps/api/tests/test_smoke.py](/Users/clint/Workspace/forms-builder/apps/api/tests/test_smoke.py):
  - exported runtime session state now has explicit regression coverage for JSON-persisted document + session roundtrip restore
  - project import/save/disk reload now has explicit smoke coverage for authored runtime metadata, payload shapes, and nested listener/action config persistence
- Deepened runtime preview interactivity in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - text, textarea, select, radio, checkbox, date, number, phone, and email fields now bind directly to the mounted runtime session instead of rendering as static shells
  - preview cards now reflect runtime hidden/disabled/required state and surface field-level validation messages inline
  - runtime field edits now dispatch real `field.change` events and revalidate the preview when QA is already in a validation/submission error loop
- Locked the web dev server to the standard port in [apps/web/vite.config.ts](/Users/clint/Workspace/forms-builder/apps/web/vite.config.ts):
  - Vite now uses `strictPort: true`, so `5173` fails fast instead of silently switching to another port
- Reworked the frontend shell in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx) around a tool-style entry flow:
  - top-level navigation is now `home`, `review`, and one main `workspace` instead of `Import + Review`, `Build`, and `Publish`
  - the app now opens to a real start screen with `New` and `Open` as the primary entry points
  - `New` now branches into `Blank form` and `Import PDF`
  - recent persisted projects can be reopened directly from the start screen, and `Open JSON` remains a first-class path
  - publish/release details moved out of stage navigation and into a workspace-level `Project details` modal with save/publish actions and stored-artifact visibility
- Refined the PDF-backed creation flow in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - review now reads as `Import Review` with creation-oriented copy instead of the old standalone intake/workspace framing
  - the review header now presents `Back to Home` and `Replace PDF` actions rather than mixing in unrelated top-level intake paths
  - source preview copy now treats the PDF as the active source for a project-creation step, not as a generic app-wide import area
  - the right rail now includes an explicit three-step creation-flow card and clearer `Create project workspace` language
  - review action labels were tightened from `Looks good enough to build` / `Promote into builder` to review-state and workspace-creation language
- Completed the base Phase 3-6 behavior-workspace reset in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - Phase 3: added a dedicated `Rules Manager` full index that treats rules, listeners, and event flows as first-class objects with search plus step, scope, trigger, effect/action, status, `Impacts this field`, and `Started from this field` filters
  - Phase 4: demoted the graph into a secondary visualization/debug workspace, renamed the advanced tab to `Graph view`, and routed graph/header/node actions back through `Rules Manager` or the focused studio instead of graph-first creation
  - Phase 5: made the simulator a behavior-workspace runtime loop with selected-target context, `Test this rule`, `Test this chain`, and `Show latest runtime effect` affordances, leaving deeper trace/debug behind the advanced summary
  - Phase 6: moved source evidence into an on-demand `Source Compare Workspace` overlay, kept source compare/provenance as a support tool, and removed the persistent fourth-column source burden from the main workspace grid
  - fixed duplicate React keys in the source compare evidence list by keying visible source groups/fields with page, section, source id, and visible index context
- Deepened the `Rules Manager` object lifecycle in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - full-index rows now expose a focused `Object detail` card instead of forcing every lifecycle control into the cramped row
  - document-wide state rules, runtime listeners, and event flows can now be enabled/disabled, duplicated, and deleted from the manager
  - status filters now reflect explicit disabled state for conditional rules and runtime listeners/flows
  - row-level `Test` still hands directly into the simulator/runtime loop with the selected object in context
- Tightened disabled-rule runtime semantics in [packages/runtime/src/engine.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.ts) and [packages/runtime/src/engine.test.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.test.ts):
  - disabled conditional rules no longer gate runtime listeners
  - added deterministic runtime coverage for that disabled-guard behavior
- Completed the next graph cleanup pass in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx) and [docs/behavior-workspace-spec.md](/Users/clint/Workspace/forms-builder/docs/behavior-workspace-spec.md):
  - graph view now presents itself as secondary visualization/tracing/debugging rather than the primary authoring surface
  - graph-local create/delete/grow controls were replaced with `Open Rules Manager`, `Return to studio`, `Manage flow`, `Edit chain in studio`, and `Open lifecycle details` handoffs
  - the graph selection panel now reads as `Inspect here, edit in Studio`, with focused Studio/Rules Manager actions instead of inline graph editing
  - the behavior workspace spec now records Studio/Rules Manager ownership and explicitly keeps graph mutation out of the normal creation/lifecycle path
- Added persistence-grade behavior lifecycle coverage:
  - `ConditionalRule.enabled` is now part of the backend model and generated schema contract, so disabled rules survive API save/reload instead of being silently dropped
  - [apps/api/tests/test_smoke.py](/Users/clint/Workspace/forms-builder/apps/api/tests/test_smoke.py) now covers enable/disable, duplicate, and delete-style mutations for a conditional rule, a form-level listener, and a field event flow across `PUT /projects/{id}/document` plus disk reload
  - [docs/behavior-workspace-spec.md](/Users/clint/Workspace/forms-builder/docs/behavior-workspace-spec.md) now captures the first behavior-tooling pruning recommendations after the Studio/Rules Manager/Graph/Simulator split
- Completed the first behavior-tooling pruning pass in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx) and [docs/behavior-workspace-spec.md](/Users/clint/Workspace/forms-builder/docs/behavior-workspace-spec.md):
  - `Behavior Studio` now defaults to the full `Rules Manager` index when authored behavior already exists, instead of reopening as another broad stacked workspace
  - `Graph view` keeps document-level map controls hidden until the user explicitly enters `Document graph workspace`
  - simulator raw session/payload/trace tools now sit behind a tighter advanced disclosure, keeping selected-rule/chain testing and authored evidence as the primary runtime loop
  - source compare actions are now contextual to imported provenance or retained import issues, so source does not stay visible as a generic side burden
- Reset the Behavior Studio navigation model in [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx):
  - moved the compact behavior toolbar onto the selected section, group, or field card for selected-context `Add rule`, `Add listener`, `Add event`, and `Test` actions
  - tightened Behavior Studio into smaller mode-sized shells that anchor above/below the selected behavior button when viewport space allows, draw a small pointer back to the clicked action, and lock background body scrolling so create/test work no longer competes with the step preview scroll
  - stabilized the anchored Studio shell across `Create`, scoped `Manage`, and scoped `Test`; `Manage` now defaults to selected-object rules/listeners/events with `Open full manager` as the document-wide escape hatch
  - replaced the broad advanced simulator render inside the popover with a selected-object simulator mini-panel and moved raw traces/session/host-loop work behind `Open runtime lab`
  - removed the stacked full-width creation buttons from the right rail so the rail stays inspection/status oriented
  - changed Behavior Studio into a top-aligned viewport workbench with internal scrolling, Escape close, initial focus, and focus restoration
  - split Studio into explicit `Create`, `Manage`, `Test`, and `Graph` modes so rule/event creation no longer opens into a manager-plus-editor vertical stack

## Key Files

- [apps/api/src/form_builder_api/main.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/main.py)
- [apps/api/src/form_builder_api/repository.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/repository.py)
- [apps/api/src/form_builder_api/models/authoring.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/models/authoring.py)
- [apps/api/src/form_builder_api/services/authoring.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/services/authoring.py)
- [packages/schema/src/authoring.ts](/Users/clint/Workspace/forms-builder/packages/schema/src/authoring.ts)
- [packages/schema/src/runtime.ts](/Users/clint/Workspace/forms-builder/packages/schema/src/runtime.ts)
- [apps/web/src/App.tsx](/Users/clint/Workspace/forms-builder/apps/web/src/App.tsx)
- [apps/web/src/lib/api.ts](/Users/clint/Workspace/forms-builder/apps/web/src/lib/api.ts)
- [apps/web/src/lib/authoring-utils.ts](/Users/clint/Workspace/forms-builder/apps/web/src/lib/authoring-utils.ts)
- [apps/api/src/form_builder_api/models/runtime.py](/Users/clint/Workspace/forms-builder/apps/api/src/form_builder_api/models/runtime.py)
- [packages/runtime/src/engine.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/engine.ts)
- [packages/runtime/src/document-index.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/document-index.ts)
- [packages/runtime/src/session-state.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/session-state.ts)
- [packages/runtime/src/validation.ts](/Users/clint/Workspace/forms-builder/packages/runtime/src/validation.ts)
- [docs/runtime-authoring-guide.md](/Users/clint/Workspace/forms-builder/docs/runtime-authoring-guide.md)
- [docs/runtime-host-integration.md](/Users/clint/Workspace/forms-builder/docs/runtime-host-integration.md)
- [docs/runtime-cookbook.md](/Users/clint/Workspace/forms-builder/docs/runtime-cookbook.md)

## Last Verified

- Latest command pass:
  - `npm run typecheck:web`
  - `npm run build:web`
  - result: passed; Vite still reports the existing `>500 kB` chunk-size warning
- Previous runtime/contract pass:
  - `npm run build:runtime`
  - `npm run test:runtime`
  - result: runtime tests `7 passed`
- Backend:
  - `PYTHONPATH=apps/api/src ./.venv/bin/pytest apps/api/tests/test_smoke.py apps/api/tests/test_extraction.py apps/api/tests/test_extraction_adapters.py`
  - result: `49 passed`
- Frontend:
  - `npm run build:schema`
  - `npm run build:runtime`
  - `npm run test:runtime`
  - `npm run typecheck:web`
  - `npm run build:web`
- Workspace:
  - `npm install`
- Backend:
  - `PYTHONPATH=apps/api/src ./.venv/bin/pytest apps/api/tests/test_smoke.py`
- Runtime:
  - `npm run test:runtime`
  - result: `5 passed`
- Backend:
  - `PYTHONPATH=apps/api/src ./.venv/bin/pytest apps/api/tests/test_smoke.py`
  - result: `8 passed`
- Frontend:
  - `npm run typecheck:web`
  - `npm run build:web`
- Browser:
  - live pass on `http://127.0.0.1:5173/` validating the repaired `@form-builder/runtime` workspace import, runtime-driven builder step progression from `Continue`, and the new form-level `Events` presets/listener editor
  - live pass on `http://127.0.0.1:5173/` validating the `Runtime tools` modal, session snapshot / submit payload / trace visibility, step-to-step runtime navigation, and `form.validation_failed` emission on preview submit
  - live pass on `http://127.0.0.1:5173/` validating that the runtime trace no longer duplicates `form.load` / `step.enter` entries after the engine fix
  - live pass on `http://127.0.0.1:5173/` validating `Events` payload editing in both key/value and JSON modes for `emit_event`
  - live pass on `http://127.0.0.1:5173/` validating the denser shell treatment, updated iconography, tighter builder chrome, and default shadcn-style app surface
  - live pass on `http://127.0.0.1:5173/` validating authored button dispatch through explicit runtime listeners, seeded-value submit through `Runtime tools`, `form.submit` hold-in-place behavior, and both `form.submit_success` and `form.submit_error` roundtrip responses
  - live pass on `http://127.0.0.1:5173/` validating form-level multi-action submit listeners, authored key/value + JSON payload usage, runtime trace visibility for `form.submit.dispatched` and `host.action_requested`, and mock-host success completion after save/reload on the clean standard-port stack
  - live pass on `http://127.0.0.1:5173/` validating the new `Project Home` start screen, `New` / `Open` entry framing, the `Blank form` / `Import PDF` branching, and removal of `Publish` as a primary navigation state
  - live pass on `http://127.0.0.1:5173/` validating the updated `Project Home` shell after the review-flow copy cleanup and confirming the entry framing still reads correctly after the import-review refactor
  - live pass on `http://127.0.0.1:5173/` validating the new in-workspace `Open` modal, direct saved-project switching without bouncing through Home, and clean restart/cleanup on the fixed `5173` / `8000` stack
  - live pass on `http://127.0.0.1:5173/` validating a full generated-PDF import -> review -> promote flow, the imported-project landing card inside the workspace, `Open source reference` handoff copy, and current-step source-page highlighting inside the reference drawer
  - live pass on `http://127.0.0.1:5173/` validating the compact `Creation Preflight` review surface with a generated local PDF, including the tighter summary-first rail, mark-reviewed gate, and end-to-end promote into the imported-project workspace
  - live pass on `http://127.0.0.1:5173/` validating in-workspace `Compare source` affordances from the current step and section selection, plus selection-aware source-reference filtering and matching imported section/field summaries inside the drawer
  - live pass on `http://127.0.0.1:5173/` validating reverse source-linking from the source-reference drawer back into authored selection, including section, group, and field clicks that retarget the inspector and keep the compare drawer open on the clean standard-port stack
  - live pass on `http://127.0.0.1:5173/` validating the new `History` workspace surface on a multi-revision project, including opening an older revision snapshot into the builder, showing the revision-view banner/state, and returning cleanly to the latest saved project head on the standard-port stack
  - live pass on `http://127.0.0.1:5173/` validating unsaved-change guardrails for the new workspace/history model, including discard/reload prompts when opening a revision snapshot, returning to the latest saved head, switching projects through `Open`, and returning home from the open-workspace shell
  - live pass on `http://127.0.0.1:5173/` validating the remaining destructive-entry guardrails: dirty-workspace prompts plus successful discard transitions for `New > Blank form`, `Open > Open JSON`, `New > Import PDF`, and `Open > Recent imports > Resume import`; the lightweight browser `beforeunload` warning is now implemented for dirty workspace state, though it was not separately browser-automated in this turn
  - live pass on `http://127.0.0.1:5173/` validating the second builder interaction pass on the standard-port stack: blank authoring now shows real drop-target empty states (`No groups in this section yet`, `No sections in this step yet`) instead of dead whitespace, and drag/drop affordances now include dedicated insertion-marker surfaces in the canvas/step strip rather than relying only on card bodies as implicit drop targets
  - live pass on `http://127.0.0.1:5173/` validating the new inspector `Map` surface: whole-document counts for steps/rules/listeners, form-level runtime listener summaries, per-step path/runtime cards, and jump actions from the map into the form-level Events editor
  - live pass on `http://127.0.0.1:5173/` validating the first behavior-workspace transition pass: the inspector now reads `Properties` / `Behavior` / `Map`, the width toggle is removed, `Form behavior` replaces `Form events`, and the workspace still opens cleanly on the standard-port stack after the tab-model rename
  - live pass on `http://127.0.0.1:5173/` validating the next behavior-authoring slice: selecting a real imported field, opening `Behavior`, clicking `Add rule`, and staying anchored on the same field while the new inline rule editor opens inside the inspector; this also confirmed the starter-palette-first `Behavior` flow and exposed/fixed the runtime `step.enter` selection-clobbering bug that had been collapsing same-step field selections back to the step on document edits
  - live pass on `http://127.0.0.1:5173/` validating the first shared behavior-graph slice: on a real imported field, `Behavior` now shows one graph surface plus one docked composer; `Show or hide this field` generates a state-flow chain and lands directly in the rule composer, while `Emit event on change` generates an interaction-flow chain and lands in the listener/action composer without losing the field selection
  - live pass on `http://127.0.0.1:5173/` validating the first in-context simulator slice: the header `Simulator` button now takes the workspace into the `Behavior` simulator surface instead of opening a separate modal; on a real imported field, `Fill required -> Run submit -> Simulate success` now resolves entirely inside the behavior workspace, with latest runtime status, host-loop controls, and advanced payload/trace/session debug exposed in one place
  - live pass on `http://127.0.0.1:5173/` validating the next graph/map unification slice: on a real imported field, the `Map` now opens authored rules and listeners directly into the shared `Behavior` graph, the matching graph node lands focused in the docked composer, and the graph now renders clearer `When` / `Then` edge labels for both state and interaction chains
  - live pass on `http://127.0.0.1:5173/` validating the next graph-native authoring slice: after seeding one state rule and one interaction flow on `1. A. LAST BRANCH OF SERVICE`, the graph can now grow in place without returning to the starter palette; the `Grow state flows` controls added a second rule directly from the graph, and `Add Host action` appended a second action node into the existing interaction chain and focused it in the docked composer
  - live pass on `http://127.0.0.1:5173/` validating the next behavior-workspace ergonomics slice: the `Behavior graph` is now the first major surface in the inspector instead of sitting below the starter content, `Quick starts` only appears when no flow exists yet, and the runtime action editor now uses clearer `Event payload` / `Request payload`, `Structured fields` / `Raw JSON`, and `Host handler key` language while still allowing host-action growth directly from the graph
  - live pass on `http://127.0.0.1:5173/` validating the deeper simulator/payload-authoring slice on `1. A. LAST BRANCH OF SERVICE`: `emit_event` and `host_action` structured payload editors now expose explicit field types (`Text`, `Number`, `True/false`, `JSON`, `Null`) instead of string-only key/value rows, and `Advanced session debug` now opens on summary-first cards (`Session tools`, `Runtime summary`, `Current submit payload`, `Current session snapshot`, `Recent runtime events`) with trace/payload/session sizes surfaced before the raw JSON blocks
  - live pass on `http://127.0.0.1:5173/` validating the next composer-guardrail slice on `1. A. LAST BRANCH OF SERVICE`: field-scoped interaction starters now seed graph/composer defaults with stable event and host names instead of generic `custom.event` / `host.action`, the listener/action composer now exposes suggestion chips for trigger keys, emitted event names, and host handler keys, and forcing an invalid host key (`Bad Name`) now shows an inline naming warning instead of silently accepting it
  - live pass on `http://127.0.0.1:5173/` validating the common-flow payload-template slice: on `1. A. LAST BRANCH OF SERVICE`, `Field changed` now stamps a structured event payload starter with `fieldId`, `fieldKey`, `stepId`, and `changeOrigin`, `Host lookup` stamps a structured host-request payload starter with `fieldId`, `fieldKey`, `query`, and `requestSource`, and under `Form behavior`, `Emit event on submit` now offers and successfully applies a `Form submit dispatched` payload starter with `formId`, `projectId`, and `submitOrigin`; the host-action composer also now surfaces `Host prefill` alongside `Host lookup` when those field-level flows are relevant
  - live pass on `http://127.0.0.1:5173/` validating the next graph-ergonomics slice on `1. A. LAST BRANCH OF SERVICE`: the `Behavior graph` now has explicit `All flows` / `State rules` / `Interaction flows` filtering plus `Focus mode` / `Overview mode`, the graph-growth helpers now respect the active filter instead of showing unrelated affordances, and overview cards now carry denser hierarchy cues (`Watch ...`, `Effect ...`, `Trigger ...`, action counts, enabled state) so behavior can be scanned more like a workspace map before dropping into the composer
  - live pass on `http://127.0.0.1:5173/` validating the next graph-navigation slice: from `Behavior` overview mode, the new `Overview navigator` now exposes document-wide form runtime plus step-level behavior jump targets, `Open form runtime` correctly moves from node-level behavior into document-level orchestration, `Open flows` on the step card correctly returns into the step’s authored behavior scope, and `Open full map` now hands directly into the long-form `Map` surface when deeper document scanning is needed
  - live pass on `http://127.0.0.1:5173/` validating live runtime-value mappers on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Field changed` and `Host lookup` now stamp structured payload rows with `Runtime ref` values for current field id/key, current step id, and current runtime value, typing into the live runtime preview drove the listener through to `host.action_requested`, and deterministic runtime tests now prove those authored payload refs resolve against the active session context at execution time
  - live pass on `http://127.0.0.1:5173/` validating the next map-language slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Map` now defaults to a new `Graph overview` mode while preserving `Summary list` as a fallback, the document and step surfaces now reuse the same trigger/action graph language as `Behavior`, and a real Step 3 interaction flow seeded in `Behavior` showed up in `Map` and successfully round-tripped back into the focused graph/composer through `Open in graph`
  - live pass on `http://127.0.0.1:5173/` validating the next simulator-evidence slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the simulator now exposes a structured `Latest authored evidence` card for the newest emitted custom event or host action, including source label, correlation id, and resolved payload rows, and the live pass confirmed both `Emit event on change` and `Host lookup` flow through that card without opening raw advanced-debug JSON; this pass also exposed and fixed a hook-order regression caused by placing a memoized evidence helper inside the behavior render path
  - live pass on `http://127.0.0.1:5173/` validating the next simulator-evidence follow-through on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Recent runtime events` now acts as a selectable authored-evidence trail, so clicking the older emitted event loads its payload/source/correlation detail into the simulator evidence card, the selected row flips to `Viewing evidence`, and `Back to latest` correctly restores the most recent host-action evidence without leaving the behavior workspace
  - live pass on `http://127.0.0.1:5173/` validating the next adjacent-context simulator slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the selected host-action evidence now shows its immediately preceding emitted event in a dedicated `Adjacent trace context` card, and switching onto that emitted event shows both the newer `host.action_requested` and the older `field.change` trigger without dropping back into the raw event list
  - live pass on `http://127.0.0.1:5173/` validating the next grouped-chain simulator slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the simulator now compresses authored runtime evidence into a small `Authored trace chain`, so the selected host action reads as `field.change -> emitted event -> host action`, and switching onto the emitted event flips the same grouped chain to `field.change -> selected emitted event -> host action follow-up`
  - live pass on `http://127.0.0.1:5173/` validating the grouped-chain follow-through on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the simulator trace chain now labels the built-in source event correctly as `Trigger`, keeps the currently selected evidence step marked `Active`, and reads cleanly as `field.change -> emitted event -> host action` for the host-action view and `field.change -> selected emitted event -> host action follow-up` for the emitted-event view
  - live pass on `http://127.0.0.1:5173/` validating multi-chain simulator scanning on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Recent runtime events` now opens with grouped `Authored chain summaries`, so multiple correlation-scoped `field.change -> emitted event -> host action` sequences can be compared side by side before drilling into one evidence item, and clicking `Inspect chain` correctly hands that grouped sequence back into the detailed simulator evidence card
  - live pass on `http://127.0.0.1:5173/` validating broader runtime/session payload references on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the `Field changed` starter now stamps live runtime refs for `stepTitle`, `projectId`, and `sourceNodeType` in addition to the earlier field/step/value refs, and the structured payload editor now exposes the expanded runtime-ref option set (`Current step title`, `Current form title`, `Current project id`, `Current source node id`, `Current source node type`) directly in the real composer UI
  - live pass on `http://127.0.0.1:5173/` validating the next graph-canvas slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding two real interaction flows on the same field, `Behavior` focus mode now exposes a `Flow navigator` with direct jump targets for each flow, swapping between those targets correctly reduces the graph back down to one focused chain at a time, and `Overview mode` still expands back out into the denser multi-flow scan surface when broader comparison is needed
  - live pass on `http://127.0.0.1:5173/` validating the next graph-viewport slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: once those two interaction flows were in place, the graph toolbar now exposes live viewport/layout controls, `Fit flows` visibly recomputed the graph to `96%` in overview mode, and toggling `Comfortable lanes` to `Dense lanes` compacted the same multi-flow graph without leaving the behavior workspace
  - live pass on `http://127.0.0.1:5173/` validating the next local graph-navigation slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding the same two interaction flows, direct dragging was exercised on `Behavior flow canvas`, `Zoom +` moved the viewport to `110%`, and `Reset view` cleanly returned the local graph back to `100%` without leaving the behavior workspace
  - live pass on `http://127.0.0.1:5173/` validating the next document-level graph-navigation slice on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the new `Document navigator` now stayed inside `Behavior` while jumping from Step 3 out to Page 6 through `Open step`, back into the authored Step 3 graph through `Open flows`, and into form-level behavior through `Open form behavior`, with the graph viewport recentering on each jump instead of leaving the user in stale local graph state
  - live pass on `http://127.0.0.1:5173/` validating the larger `Map + Graph Unification` chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding a live Step 3 interaction flow, `Map` now opens the graph with a visible `Graph context` strip (`Opened from Map`, source pill, current location), and `Back to map` returns to the same map view with that authored Step 3 flow still visible, so the handoff now reads as one continuous workspace instead of two loosely related screens
  - live pass on `http://127.0.0.1:5173/` validating the next larger graph-workspace maturity chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: step-level `Behavior` now exposes `Scope clusters` so larger authored behavior reads by scope instead of one long repeated stack, form-level `Behavior` now clusters by step across the document, and opening the clustered Step 3 flow now lands back in the focused graph with a visible `Opened from Scope clusters` context strip
  - live pass on `http://127.0.0.1:5173/` validating the next larger behavior-authoring chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Behavior` can now seed grouped state bundles (`Show and require together`), the graph now collapses shared state predicates into one `Conditional bundle` with multiple effect nodes plus related-effect expansion in the rule composer, and interaction authoring now supports multi-action starter chains (`Emit then request host action`) with a clearer chain editor (`Chain path`, common-chain replacement, move earlier/later, duplicate, and insert-after controls) inside the same docked composer
  - live pass on `http://127.0.0.1:5173/` validating the next larger document-graph chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: `Behavior` now opens with a real `Document graph overview` that summarizes step lanes plus form runtime, surfaces live rule/flow counts at the document level, and exposes clustered `Open flows` / `Open scope` handoffs; after seeding a live `Emit event on change` flow on `3B`, the Step 3 lane reflected the authored flow and the new scope-card `Open flows` action landed back in the focused graph with visible `Opened from Document graph overview` context
  - live pass on `http://127.0.0.1:5173/` validating the next larger document-graph maturity chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the older standalone `Document navigator` has now been folded into the same `Document graph overview` surface, which now supports a stronger default `Mini-map` mode plus a fallback `Board view`; after seeding a live `Emit event on change` flow on `3B`, the new mini-map surfaced the field scope as an active orbit around the Step 3 lane and the mini-map `Open flows` handoff returned to the focused graph with visible `Opened from Document graph overview` context
  - live pass on `http://127.0.0.1:5173/` validating the next larger global-graph interaction chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the document-wide mini-map now has its own lane strip plus viewport controls (`Zoom +/-`, `Fit map`, `Reset view`), can be keyboard-panned once focused, and now supports lane-level focus toggles; after seeding a live `Emit event on change` flow on `3B`, `Zoom +` moved the mini-map to `110%`, `Reset view` returned it to `100%`, the mini-map accepted keyboard panning, and shifting lane focus off Step 3 flipped the per-lane controls from `Unfocus lane` on the current lane to `Focus lane` on the others
  - live pass on `http://127.0.0.1:5173/` validating the next larger dedicated-document-graph chunk on `Page 5 · SECTION II - MILITARY SERVICE INFORMATION (Continued)`: `Behavior` now has a real `Document graph workspace` mode that keeps the document-scale mini-map and orchestration surfaces up front while hiding the docked node-authoring cards, and `Return to authoring` restores the `Node composer` plus `Quick starts`; the live pass confirmed the new workspace split on the real `Va Form 10 10Ez` Step 3 project view without leaving the standard `127.0.0.1:5173` / `127.0.0.1:8000` stack
  - live pass on `http://127.0.0.1:5173/` validating the next larger document-graph interaction chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding a live `Emit event on change` flow on the Step 3 field, the document graph now supports direct lane expansion from both the mini-map and board surfaces, exposes a new `Expanded lane detail` panel with visible scope counts plus `Open scope` / `Open flows` actions, and the expanded-lane `Open flows` handoff now returns to the focused graph with visible `Opened from Expanded lane detail` context instead of forcing the user back through the broader card stack
  - live pass on `http://127.0.0.1:5173/` validating the next larger document-graph canvas chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding the same Step 3 `Emit event on change` flow, `Behavior` now defaults the document graph to a real `Canvas view`, adds `Cross-lane clusters` filtering (`Field scopes`, etc.), keeps that canvas alive inside `Document graph workspace`, and lets `Open flows` hand directly back into the focused graph with visible `Opened from Document graph canvas` context instead of treating the global surface like just another card list
  - live pass on `http://127.0.0.1:5173/` validating the next larger global-canvas interaction chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding the Step 3 `Emit event on change` flow, the document graph canvas now supports lane pinning plus a `Dense canvas` mode, keeps those controls available inside `Document graph workspace`, suppresses the old bottom lane-detail dependency in canvas mode, and still hands `Open flows` back into the focused graph with visible `Opened from Document graph canvas` context
  - live pass on `http://127.0.0.1:5173/` validating the next larger global-canvas orchestration chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding the same Step 3 `Emit event on change` flow, the canvas now exposes a real `Canvas navigator` with `Show on canvas` cluster actions, `Relevant lanes only` filtering, and reset controls; the field-scope navigator narrowed the global graph to the matching lane set and the same orchestration path still handed `Open flows` back into the focused graph with visible `Opened from Document graph canvas` context inside `Document graph workspace`
  - live pass on `http://127.0.0.1:5173/` validating the next larger global-canvas spatial chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: the document graph canvas now groups lanes into explicit swimlanes (`Focused and pinned lanes`, `Behavior lanes`, and `Quiet lanes`) instead of one long flat lane stack, and after seeding the same Step 3 `Emit event on change` flow the Step 3 lane moved into the top swimlane with its field-scope cluster visible while the quieter pages stayed grouped below; the same swimlane structure also persisted inside `Document graph workspace`
  - live pass on `http://127.0.0.1:5173/` validating the next larger global-canvas cross-lane chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding the same Step 3 `Emit event on change` flow, the document graph canvas now keeps an explicit `Follow this behavior across lanes` strip alive for the active cluster family, exposes direct `Open matching scope` handoffs, and marks the focused Step 3 lane as `Matches field scopes`; the same cross-lane strip also persisted inside `Document graph workspace`
  - compile plus live DOM pass validating the next broader document-traversal chunk: the document graph canvas now derives explicit `Family trails` for related cluster families, exposes lane-by-lane traversal actions (`Follow family`, `Open matching scope`, `Previous lane`, `Next lane`), and keeps those controls wired through the same `Document graph workspace` surface; the browser pass confirmed the workspace toggle plus canvas lane controls still behave after the family-trail wiring, while the new family-trail strip remains intentionally gated behind an active authored family selection
  - live pass on `http://127.0.0.1:5173/` validating the next broader whole-form traversal chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after seeding a field-scoped `Emit event on change` flow and a step-scoped listener on Step 3, the document graph now keeps a real `Whole-form trail set` alive across families, can hold both `Field scopes` and `Step scopes` in the tracked trail set at once, and can pivot the active trail family with `Previous family` / `Next family` without rebuilding the surrounding canvas context
  - live pass on `http://127.0.0.1:5173/` validating the next cross-family hotspot chunk on `3B. WHEN DID YOU SERVE IN THESE LOCATIONS?`: after keeping both `Field scopes` and `Step scopes` in the tracked trail set, the document graph now surfaces a `Shared lane hotspots` summary for the overlapping Step 3 lane and can jump directly from that hotspot into authoring with `Open field scopes`, landing back in the field graph with visible `Opened from Document graph hotspot` context
  - live pass on `http://127.0.0.1:5173/` validating the first larger behavior-shell reset on `Page 3 · Submitting Your Application`: the right-rail `Behavior` tab is now a compact launcher/manager instead of the full stacked authoring surface, `Open studio` now raises a dedicated `Behavior studio` overlay for focused rule/listener/event wiring, and `Advanced workspace` now carries the heavier graph/document-graph/simulator surfaces outside the narrow inspector column
  - live pass on `http://127.0.0.1:5173/` validating the next shell-and-studio follow-through on `Page 3 · Submitting Your Application`: the right rail is now slimmer as a `Behavior launchpad` plus `At a glance` summary, while the studio now owns the real `Rules and flows manager` with search, `All / Rules / Flows` filters, and the primary rule/flow re-entry path instead of duplicating the same launcher cards from the inspector
  - live pass on `http://127.0.0.1:5173/` validating the guided Behavior Studio creation pass on `Page 3 · Submitting Your Application`: `Add rule` now opens a guided `Create rule` setup with scope/starter/edit summary and a correct field-selection guard, `Add event flow` now opens guided event setup instead of the raw listener editor, and selecting `Blank event flow` creates a real `step.enter` interaction flow that appears in the manager and opens in the focused chain composer
  - live pass on `http://127.0.0.1:5173/` validating the base Phase 3-6 implementation: the new `Rules Manager` full index exposes step/scope/trigger/effect/status/object-view filters plus `Open in studio` and `Test` actions; `Graph view` now describes itself as secondary visualization and exposes manager/studio handoffs; `Test this chain` dispatches the selected `step.enter` flow and updates authored runtime evidence; `Source Compare Workspace` opens as an overlay instead of a cramped fourth column
  - live pass on `http://127.0.0.1:5173/` validating the next `Rules Manager` object-lifecycle pass on `Page 3 · Submitting Your Application`: a browser-only event flow opened in the full index with an `Object detail` card, `Disable` flipped the row/detail status and stayed visible under the disabled filter, `Enable` restored the flow, `Duplicate` created a second event flow and selected it, `Delete` removed the duplicate back to one unsaved flow, and row-level `Test` opened the advanced workspace/simulator with emitted-event runtime evidence for `page.3.submitting.your.application.entered`
  - live pass on `http://127.0.0.1:5173/` validating the graph cleanup pass on `Page 3 · Submitting Your Application`: graph view now shows manager/studio handoffs instead of direct creation/deletion/growth controls, `Edit chain in studio` returns to the focused composer, and `Open lifecycle details` opens the full Rules Manager index with the selected event flow detail expanded
- Persistence/contract:
  - `PYTHONPATH=apps/api/src ./.venv/bin/pytest apps/api/tests/test_smoke.py -q`
  - result: `9 passed`
  - `npm run generate:schema-types`
  - `npm run build:schema`
  - `npm run test:runtime`
  - result: runtime tests `7 passed`
  - `npm run typecheck:web`
  - `npm run build:web`

## Known Gaps

- Promotion currently requires a reviewed conversion and always derives the initial project from the current extraction draft; there is no partial-review promotion path yet.
- Project persistence is file-backed and single-user oriented; there is no concurrency model, database storage, or shared workflow yet.
- The new start/workspace shell is in place, but the import-review flow still needs a second pass:
  - review no longer carries the whole comparison burden, and source-reference compare is now bidirectional, but the imported/source surface still lacks richer spatial comparison affordances beyond list-based linking
- The builder still needs a second interaction pass:
  - logic editing is now bounded and recoverable, and the new map surface now opens directly into the shared graph with first-pass `When` / `Then` semantics; graph filtering, focus-mode flow navigation, overview mode, denser hierarchy cues, in-graph overview navigation, and a first graph-style `Map` overview are now in place, and the document-wide surface now includes a navigable mini-map with its own controls, but the graph still needs a more spatial and freely navigable global workspace once flows get larger
  - publish is now a workspace action instead of a stage, but the release model is still a status/storage surface rather than a full runtime/export pipeline
- Behavior authoring now needs a larger UX reset rather than more incremental polish:
  - the shell has now started that transition with a single `Behavior` tab, a slimmer right-rail behavior launchpad, a dedicated `Behavior studio` overlay for focused rule/listener/event wiring, guided setup paths for `Add rule`, `Add listener`, and `Add event flow`, a shared graph surface, one docked composer, grouped state bundles, multi-action chain templates, and a first-class `Rules Manager` index with object detail plus enable/disable, duplicate, and delete lifecycle controls; the remaining manager gaps are bulk grouping, safer undo/confirmation affordances, and richer field-centric impact views
  - the product direction is now Behavior Studio plus Rules Manager as the primary authoring path, with graph used for focused visualization, tracing, and debugging rather than normal rule/listener creation
  - rule/listener editing now shares one docked composer and one graph that can be seeded, extended in place, and reached from the new studio, and the map now speaks the same graph language and can hand authored nodes directly into that graph; focus mode can now collapse multi-flow selections back to one chain at a time, first-pass `Fit flows` plus lane-density controls are in place, the local viewport now supports drag/pan plus reset, the `Map -> Graph` handoff now carries visible context plus a return path, step/form scope clusters now keep larger authored behavior from collapsing into one repeated stack, the old document navigator has now been folded into the `Document graph overview`, that overview now supports clustered board, spatial mini-map, and a new default `Canvas view` plus cross-lane scope filtering, the new `Document graph workspace` mode now gives that surface its own dedicated authoring state inside `Behavior`, the document graph now supports direct lane expansion plus an `Expanded lane detail` surface with scope-level handoffs, the canvas now supports pinning, denser cluster packing, navigator-driven filtering, grouped swimlanes (`Focused and pinned lanes`, `Behavior lanes`, `Quiet lanes`), and a persistent related-lane strip with direct matching-scope handoffs, and the composer now supports grouped conditional effects plus replaceable multi-action chains, but the next gap is now finishing the studio/inspector split before pushing the global canvas much further
  - graph-first creation pressure has been reduced and lifecycle persistence now has API/disk-reload coverage; the next gap is pruning secondary graph/document/simulator/source affordances so the clearer ownership model is reflected in the visible UI
  - source provenance now opens in an explicit compare workspace, but it still needs richer spatial/visual comparison and stronger focus-management polish before it feels like a complete on-demand compare tool
- Provenance is retained, but field-level evidence is still lighter-weight in the builder than in the review stage.
- Runtime behavior authoring is still shallow:
  - the shared runtime package now exists and preview field editing now runs through it for common control types, and the first simulator surface is now in-context rather than modal, but runtime authoring still needs richer common-flow templates and stronger action-specific guardrails on top of the new typed payload editors
  - the `Behavior` workspace now supports clearer payload language, explicit structured field typing, selected-rule/chain test affordances, latest authored runtime evidence, and a less raw advanced-debug entry point, but session import/export is still dev-oriented and deeper runtime QA flows are still lighter than the authoring shell around them
  - trigger/event/handler naming is now more guided with inline validation, suggestion chips, common-flow payload starters, broader live runtime/session mappers for field/step/form/project/source-node context, and grouped chain scanning inside the simulator surface, but graph manipulation is still more constrained than the authoring model around it
  - node-level runtime presets now have deterministic engine coverage and live browser coverage for authored button dispatch, preview field interaction, and form-level multi-action payload chains, but saveable examples still need better fixture discipline if we want QA scenarios without mutating `data/projects/`
  - runtime docs now cover architecture, schema, roundtrip testing, authoring, host integration, and recipes, but they still need to evolve with stronger action-specific guardrails and a more capable host shell
- The shell is now closer to the desired direction, but there is still no explicit component-system toggle between `shadcn-style default` and `USWDS shell mode`; this pass establishes the default look rather than a live switch.

## Best Next

- Run the behavior tooling/capability review now that the ownership split is in place:
  - keep `Behavior Studio`, `Rules Manager`, and selected-object simulator testing prominent
  - tuck `Graph view`, `Document graph workspace`, advanced simulator debug, and full source compare behind explicit secondary affordances
  - identify any duplicate right-rail controls that can be removed now that Studio and workspace overlays own the heavy flows
- Follow-up after the Studio navigation reset:
  - isolate `Test` into a simulator-only panel instead of rendering the broader advanced graph workspace first
  - consider moving `Rules Manager` into its own workspace affordance if the Create/Manage split still feels too modal-heavy
  - browser-check the preview toolbar hover/focus labels and dialog focus behavior on a real project

## Product Direction Update

- The staged `Import + Review` / `Build` / `Publish` shell now needs to be treated as transitional.
- The preferred model is a traditional creation tool:
  - start with `New` or `Open`
  - `New` branches into `Blank form` or `Import PDF`
  - projects open into one main builder workspace
  - imported-source review becomes an import flow or secondary builder tool, not the top-level identity of the app
  - `Publish` remains a project status/action, not a primary application state
- Implementation plan is captured in [docs/workspace-flow-plan.md](/Users/clint/Workspace/forms-builder/docs/workspace-flow-plan.md).
- Behavior authoring direction is now captured in [docs/behavior-workspace-spec.md](/Users/clint/Workspace/forms-builder/docs/behavior-workspace-spec.md):
  - `Properties` / `Behavior` / `Map`
  - one graph system for behavior editing and overview
  - starter palette first, graph second for the default `Behavior` entry flow
  - simulator surface instead of dev-only-feeling runtime tools
  - provenance helper by default, full-width compare mode on demand

## Restart Notes

1. Start the stack with `npm run dev`.
2. Review a PDF in `Review Workspace`.
3. Mark it `Reviewed` or `Accepted`.
4. Use `Promote to project`.
5. Switch to `Authoring Workspace` to edit the persisted project.
6. Inspect saved artifacts under `data/projects/` if you need to verify disk state directly.
